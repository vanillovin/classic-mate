import { siteConfig } from '@/config/site';
import type { Metadata } from 'next';

export const metadata: Metadata = siteConfig.metaData['journey'];

export default function JourneyPage() {
  return (
    <section className='p-3 sm:p-6'>
      
    </section>
  );
}
